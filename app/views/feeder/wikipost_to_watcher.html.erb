<p><%= @recipient.username %>さん</p>

<p>ウォッチ中の局面が <%= @wikipost.user.username %>さんによって編集されました。</p>

<% if (@wikipost.position.strategy) %>
  <%= @wikipost.position.strategy.name %><br>
<% end %>
<tt><pre>
<%= @wikipost.position.to_board.to_bod %>
</tt></pre>

<p><%= link_to '編集内容ページへのリンク', ROOT_PATH + '/wikiposts/' + @wikipost.id.to_s %></p>
<br>
<p>この通知をこれ以上受け取りたくない場合は、<%= link_to '登録情報変更ページ', ROOT_PATH + '/users/edit' %>
よりメール受信を解除して下さい。(ホームページ上のタイムラインにのみ新着情報が表示されるようになります)<br>
また、この局面のウォッチを解除する場合は、<%= link_to '該当局面のページ', ROOT_PATH + '/positions/' + @wikipost.position_id.to_s %>より「ウォッチ解除」ボタンを押すか、
<%= link_to 'マイページ', ROOT_PATH + '/users/mypage' %>
にてウォッチ中の局面を管理して下さい。</p>

<%= render :partial => 'signature' %>
