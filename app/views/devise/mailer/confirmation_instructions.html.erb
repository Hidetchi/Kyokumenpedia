<p><%= @email %>様</p>

<p><%= SITE_NAME %>にご登録ありがとうございます。下記リンクよりアカウントの認証を完了して下さい。</p>

<p><%= link_to 'アカウント認証用リンク', confirmation_url(@resource, confirmation_token: @token) %><br>
( <%= confirmation_url(@resource, confirmation_token: @token) %> )
</p>
<br>
<%= render :partial => 'feeder/signature' %>
